# Скопируйте в .env и подставьте реальный URL Cloud Function.
# Префикс PUBLIC_ делает переменную доступной в браузере (import.meta.env.PUBLIC_LEAD_ENDPOINT).
PUBLIC_LEAD_ENDPOINT="https://placeholder.invalid/lead"
